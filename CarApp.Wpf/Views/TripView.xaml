<UserControl x:Class="CarApp.Wpf.Views.TripView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         Height="500" Width="720">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <WrapPanel Grid.Row="0" Margin="0,0,0,8">
            <StackPanel Width="160" Margin="4">
                <Label Content="Vælg Bil"/>
                <ComboBox ItemsSource="{Binding AvailableCars}"
                          SelectedItem="{Binding SelectedTrip.Car}"
                          DisplayMemberPath="LicensePlate"/>
            </StackPanel>

            <StackPanel Width="100" Margin="4">
                <Label Content="Distance (km)"/>
                <TextBox Text="{Binding SelectedTrip.Distance, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Width="160" Margin="4">
                <Label Content="Startdato"/>
                <DatePicker SelectedDate="{Binding SelectedTrip.StartTime, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <StackPanel Width="160" Margin="4">
                <Label Content="Startdato"/>
                <DatePicker SelectedDate="{Binding SelectedTrip.EndTime, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
        </WrapPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Tilføj" Command="{Binding AddTripCommand}" Width="90" Margin="4"/>
            <Button Content="Slet" Command="{Binding DeleteTripCommand}" Width="90" Margin="4"/>
        </StackPanel>

        <ListView Grid.Row="2"
                  ItemsSource="{Binding Trips}"
                  SelectedItem="{Binding SelectedTrip, Mode=TwoWay}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Bil" DisplayMemberBinding="{Binding Car.LicensePlate}" Width="130"/>

                    <GridViewColumn Header="Distance" DisplayMemberBinding="{Binding Distance}" Width="100"/>

                    <GridViewColumn Header="StartTime" DisplayMemberBinding="{Binding StartTime, StringFormat={}{0:dd-MM-yyyy}}" Width="140"/>

                    <GridViewColumn Header="Varighed (min)" DisplayMemberBinding="{Binding DurationMinutes, StringFormat={}{0:F0}}" Width="110"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>